# Wiki AI Chat Frontend

This project implements a simple ChatGPT-style interface for searching an internal Wiki using AI. It is built with Vue 3, Vite and Tailwind CSS.

## Development

Install dependencies and start the development server:

```bash
npm install
npm run dev
```

The application will request the API endpoint `/agent/test` using Server-Sent Events.
